<!DOCTYPE html>
<html>
<head>
	<title>Edit User</title>
</head>
<body>
	
	<% if @user.errors.any? %>
		<ul>
			<% @user.errors.full_messages.each do |error| %>
			<li><%= error %></li>
			<% end %>
		</ul>
	<% end %>

	<%= form_for @user, { method: 'patch' } do |f| %>
		<%= f.label :username, "Username: " %>
		<%= f.text_field :username, placeholder: "username" %>
		<%= f.label :email, "Email: " %>
		<%= f.text_field :email, placeholder: "example@email.com" %>
		<%= f.label :password, "Password: " %>
		<%= f.password_field :password %>
		<%= f.submit "Update User" %>
	<% end %>



	<!--
	<%= form_tag('/users', method: :put) do %>
		<div>
			<%= label_tag(:username, "Username: ") %>
			<%= text_field_tag("user[username]") %>
		</div>
		<div>
			<%= label_tag(:email, "Email: ") %>
			<%= text_field_tag("user[email]") %>
		</div>
		<div>
			<%= label_tag(:password, "Password: ") %>
			<%= password_field_tag("user[password]") %>
		</div>
		<div>
			<%= submit_tag("Create User") %>
		</div>
	<% end %>
	-->
	<!-- <form method="PATCH" action="/users" accept-charset="UTF-8">
		<p>
			<label for="username">Username: </label>
			<input id="username" type="text" name="user[username]" placeholder="username">
		</p>
		<p>
			<label for="email">Email: </label>
			<input id="email" type="text" name="user[email]" placeholder="email">	
		</p>
		<p>
			<label for="password">Password: </label>
			<input id="password" type="password" name="user[password]" placeholder="password">	
		</p>
		<p>
			<input type="submit" value="Create User">
		</p>
		<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
	</form> -->

</body>
</html>